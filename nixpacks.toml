[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "PLAYWRIGHT_BROWSERS_PATH=/app/pw-browsers playwright install --with-deps chromium"
]

[start]
cmd = "python3 setup_env.py && python3 sync/webhook.py"
